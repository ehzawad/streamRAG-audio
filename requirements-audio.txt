# Audio front-end + eval stack for streamRAG-audio.
# Installed in a SEPARATE venv (e.g. .venv-audio) — NOT the main py3.14 RAG venv,
# and NEVER co-resident with the answer LLM on the A5000. Pins match the proven
# omni-voice-lab .venv-modular stack on this box.
#
#   python3.12 -m venv .venv-audio && . .venv-audio/bin/activate
#   pip install -r requirements-audio.txt
#
# TTS synthesis (Qwen3-TTS) is GPU-sole-tenant and offline; ASR (faster-whisper)
# is offline full-clip, CPU int8. Both write to data/audio_crag_qwen/ (wav gitignored).
#
# Query-audio synthesis uses Qwen3-TTS (the external `qwen-tts` package, run in
# its own .venv-qwen-audio); it is not pinned here because it is not co-resident
# with this ASR/eval stack.

torch==2.6.0
faster-whisper==1.2.1      # offline full-clip ASR (CPU int8)
jiwer>=3.0                 # WER / entity-WER of synthetic audio vs gold text
soundfile>=0.12            # deterministic wav I/O
numpy>=1.26,<3
